import React from "react";
import ReactDOM from "react-dom";

import PinlyApp from "./pinlyApp";

ReactDOM.render(
  <React.StrictMode>
    <PinlyApp />
  </React.StrictMode>,
  document.getElementById("root")
);
